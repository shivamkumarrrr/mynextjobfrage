# Required by api/send-email.ts (SMTP2GO relay for candidate applications).
# Set these as real values in Vercel → Project → Settings → Environment Variables.
# Never put real values in this file — it's the only .env* file committed to git.

SMTP2GO_API_KEY=
SMTP2GO_SENDER=
SMTP2GO_RECIPIENT=
